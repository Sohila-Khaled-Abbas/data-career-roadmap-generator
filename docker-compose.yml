version: '3.8'

services:
  roadmap_app:
    build: .
    container_name: roadmap_app
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
      - ./output:/app/output
    environment:
      - PYTHONUNBUFFERED=1
